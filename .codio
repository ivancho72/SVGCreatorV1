{
// Run button configuration
  "commands": {
    "Run Local Server": "grunt serve",
    "Build": "grunt build --force",
    "Deploy Heroku" : "cd dist && git add . && git commit -a -m 'deployed from codio' && git push heroku master"
  },

// Preview button configuration
  "preview": {
        "Codio URL": "http://{{domain}}:9000/",
        "Deployed URL": "http://svgcreatorv1.herokuapp.com/",
        "Current File (static)": "https://{{domain}}/{{filepath}}"
  }
}